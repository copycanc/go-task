# Сервис TASKS

Команда сборки образа с приложением:
```
docker build -t task-app:{x} .
```

Структура файла .env 
```
POSTGRES_USER=***
POSTGRES_PASSWORD=***
POSTGRES_DB=***
```

Собрать приложение
```
docker compose up
```

